'use client'
import React from 'react';


const AddTocard = ()  => {
    return (
        <button onClick={ (e) => console.log(e)}>
            Add to cart
        </button>
    );
}

export default AddTocard;